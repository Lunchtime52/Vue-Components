import { ref } from "vue";
import { b as breadcrumbGlobalIndexKey, a as breadcrumbPluginKey } from "../../../keys-BZewGSMQ.mjs";
const breadcrumbPlugin = {
  install(app) {
    const items = ref({
      [breadcrumbGlobalIndexKey]: []
    });
    const reset = (key = breadcrumbGlobalIndexKey) => {
      items.value[key] = [];
    };
    app.provide(breadcrumbPluginKey, { items, reset });
  }
};
export {
  breadcrumbPlugin
};
//# sourceMappingURL=index.mjs.map
